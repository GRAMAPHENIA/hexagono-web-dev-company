{
    "version": 2,
    "buildCommand": "prisma generate && next build",
    "installCommand": "pnpm install",
    "builds": [
        {
            "src": "package.json",
            "use": "@vercel/next"
        }
    ]
}